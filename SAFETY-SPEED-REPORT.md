# 0.8.1 安全与速度验证记录

验证日期：2026-09-07。发布日期：2026-09-09。已在 Pixel 4 XL 覆盖安装；[GitHub Release](https://github.com/Vickylines/Pixel4xl-2DFaceUnlock/releases/tag/v0.8.1)。

## 基线和交付

- 实际手机基线：`0.8.0-dual-signal`，versionCode 18，v3 双信号模板。
- 源码基线提交：`cc8fc53a66de4b9bf9c395b5056aa48a9e5f5fac`。
- 新安装版本：`0.8.1-safety-speed`，versionCode 19，Release，`debuggable=false`。
- [Release APK](https://github.com/Vickylines/Pixel4xl-2DFaceUnlock/releases/download/v0.8.1/Pixel2DFaceUnlock-coral-0.8.1-safety-speed.apk)，SHA-256：`5b52d0b1e76e510a5314089b2dce698d245413957a5f4da13421e8724fdeabe5`。
- 签名校验通过，与原安装证书一致；手机上安装 APK 的 SHA-256 也与交付产物一致。
- 原 APK、旧源码和手机私有配置保存在本地私有备份中，不纳入 Git 或 Release。升级后逐项比对确认模型、启用状态、识别档位和动画偏好保持一致；不用重新录入。

## 修改及取舍

1. **识别证据有时效。** 基于相机采集时间使用最多 6 次、900 ms 的投票窗口，帧间超过 350 ms 清空；重复/倒退时间戳拒绝，检测处理超过 750 ms 的帧丢弃。闭眼、丢脸、多脸、关键点缺失以及姿态/画质硬门槛失败清空旧票，防止单个新帧借用早前通过的结果。
2. **普通路径保留完整身份校验。** 至少 5 次观察、4 帧匹配，最近两帧必须连续匹配，并检查均值、波动和至少 90 ms 采集间隔。纹理、几何和区域阈值没有放宽。闭眼或差画质后的恢复可能需要重新积累几个好帧，这是明确的安全取舍。
3. **高置信路径才提前一帧。** 连续 4 帧都满足额外纹理 0.05 余量、几何均值/峰值更严格限制、至少 44/49 局部与 24/25 核心区域、双眼概率均 ≥0.85、无画质/姿态/取景惩罚才允许提前确认。原先第五次观察不会对这类帧强制增加等待。此分支已由单元测试覆盖；本次自然光线下的真人记录走普通路径，不能把实测速率提升归因于此分支。
4. **睁眼与会话边界更严格。** 普通双眼概率门槛由 0.55 提至 0.70，拒绝缺失或非有限/越界概率。解锁结果 18 秒过期、一次消费，异步提交前复查唤醒代次、屏幕交互和锁定状态，保留系统强认证检查。错误令牌不会抹掉合法的相机启动授权。
5. **减少开销及竞态。** 合并重复配置读取，锁住完整读改写，限制心跳写盘频率；比较纹理时去掉每帧距离数组和重复权重求和，静默解锁不再构造不可见界面的格式化文本。相机取消后不重新绑定，只解绑自己的用例。模板数值损坏或保存失败明确失败。
6. **日常安装禁用调试。** Release 构建保持原签名，关闭 Android 调试入口和默认逐帧诊断。签名材料没有上传。相机隐私指示与 Android 16 输入层修复保留。

## 自动验证

- 35 项 JVM 单元测试通过，0 失败、0 错误。覆盖身份拒绝、异常模型、双眼信号、采集时效、快速路径限制、会话重复/过期/新唤醒以及异步取消。
- 真机 Instrumentation：30 项检查通过。只使用合成模型和独立缓存目录，验证模板往返、并发设置更新、AtomicFile 备份恢复、锁定计数、令牌单次消费/失效、错误令牌保护以及保存失败。测试 APK 已卸载。
- Release 构建成功，Lint 0 错误、10 警告。警告涉及既有 UI 写法、可升级依赖、ChromeOS ABI 和导出 Provider；Provider 仍由调用 UID 和 SystemUI 包身份检查保护。
- 真机 `run-as com.yusd.pixel2dface` 返回不可调试；普通 ADB shell 读取 Provider 返回 `SecurityException: Caller is not SystemUI`。
- 最终 Manifest 无网络权限，保留禁止备份。依赖版本未做大版本迁移。

## 真人验证

设备：Pixel 4 XL / coral，Android 16 / API 36，LineageOS `23.2-20260320-NIGHTLY-coral`。使用原 v3 模板及严格档位（0），纹理校准阈值 0.28、几何阈值 1.35。由用户本人配合，日志核对；没有采集照片或录像。

| 项目 | 结果与证据 |
| --- | --- |
| 初次 0.8.1 安装后的正常解锁 | 3/3 正常，用户确认无白闪或触摸异常。首帧至识别成功 549 / 283 / 269 ms；首轮启动开销单列，不当作稳态数据。 |
| 双眼闭合 | 用户确认没有成功；28 条眼睛拒绝诊断覆盖 19:11:52.371–19:12:03.783，持续约 11.4 秒，随后会话超时，无成功结果。 |
| 单眼闭合 | 用户确认没有成功；28 条拒绝诊断覆盖 19:13:27.558–19:13:38.924。首条闭合眼/睁开眼概率为 0.08/0.94，始终不进入身份成功。 |
| 识别成功后快速重锁 | 19:15:31.757 首轮成功，第二轮闭眼的拒绝诊断覆盖约 11.5 秒后超时；用户确认第二轮未成功，未复用旧认证。 |
| 系统锁定模式 | 执行系统强认证命令，19:20:06.449 的 SystemUI 日志明确因需要 PIN/强认证跳过人脸；用户随后确认已在手机解锁并重新连接。命令执行后 ADB 一度中断，未把中断本身作为安全结果。 |

## 最终速度

最终版本关闭逐帧诊断后，用户再完成 3/3 正常睁眼解锁，时间为 19:34:58–19:35:06。全部走普通路径：

| 版本/口径 | 3 次耗时 | 中位数 |
| --- | --- | --- |
| 原 0.8.0，首帧至成功 | 386 / 324 / 381 ms | 381 ms |
| 原 0.8.0，相机启动请求至成功 | 882 / 796 / 949 ms | 882 ms |
| 最终 0.8.1，首帧至成功 | 384 / 230 / 220 ms | 230 ms |
| 最终 0.8.1，相机启动请求至成功 | 815 / 526 / 507 ms | 526 ms |
| 最终 0.8.1，捕获 Activity 创建至成功 | 860 / 541 / 534 ms | 541 ms |

本次两组各 3 次的首帧后耗时中位数减少 151 ms（约 40%）；这只是此次实测差值，不能保证一般性能增益。最终后两次均在 5 帧完成，初次等待和曝光稳定仍可能增加耗时；末组初始化为 12 / 10 / 10 ms。

末组用户确认操作正常，SystemUI 日志对应记录成功并等待上滑，Android 16 相机宿主输入层处理继续生效。测试完毕恢复原日志属性，模型、偏好再次比对不变，失败计数和临时锁定均为 0。

`cameraMs` 从首个分析帧到记录成功；`sessionMs` 从请求 CameraProvider 到成功；新版 `totalMs` 从捕获 Activity 创建开始。它们都不是用户按电源键到进入桌面的总时长，还需考虑 SystemUI 固有 620 ms 等待、Activity 启动和上滑操作。保留 620 ms 是为了维持原先避免白闪的行为。

## 结论边界

本次只验证单台设备、单个已录入用户和有限光线条件下的回归。小样本耗时受距离、姿态、曝光、设备负载和热身影响，不能推导所有环境的固定加速比例。没有陌生人群样本、照片/视频攻击测试或跨版本真机覆盖，不能给出误识率或认证强度承诺。

RGB 2D 人脸仍可能被照片或视频重放绕过；睁眼检查不等于可靠活体检测。本次没有新增应用层 Keystore 加密，模板依赖 Android 私有目录和设备存储加密，root 进程仍可读取或修改。适用于便利解锁，不能替代 PIN 的安全强度。

## 本地证据

`test-artifacts/safety-speed-20260907/` 保存构建日志、测试汇总、源文件哈希、签名结果和不含人脸模板的相关验证日志（Git 忽略）。历史 0.8.0 报告仍保留在 `TEST-REPORT.md`，本报告单独记录本次 0.8.1 验证。
