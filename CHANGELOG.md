# 更新日志

## 0.8.1 - 2026-09-09

真机验证完成于 2026-09-07。

- 在 0.8.0 / v3 模板基础上调整，不覆盖旧版本功能或要求重新录入。
- 增加有时限、基于采集时间的投票，闭眼和关键质量失败清空历史，普通路径要求最近两帧匹配。
- 对明显匹配的连续好帧开放严格的四帧确认路径；保持原有身份阈值，并提高睁眼概率门槛。
- 会话结果单次消费、过期和唤醒代次校验，取消后的异步结果不再提交认证。
- 合并配置读取，完整锁定模板读改写，验证模型数值，传播录入保存失败；修复相机取消和解绑竞态。
- 默认构建不可调试的 Release APK，逐帧日志默认关闭；签名保持与现有安装一致。
- 增加安全边界单元测试及在合成数据隔离目录运行的 Android 存储回归。实测见 [SAFETY-SPEED-REPORT.md](SAFETY-SPEED-REPORT.md)。

## 0.8.0 - 2026-08-30

- 修复 Android 16 透明相机宿主的全屏输入接收层阻断锁屏触控；绿色相机隐私点出现时不再导致锁屏无法操作。
- 录入模板升级为 v3：结合 49 区局部 LBP 纹理稳定度、25 区核心面部约束和眼鼻嘴 7 维归一化几何比例。
- 每帧必须同时通过纹理、局部区域、核心区域和几何门槛，之后才能参与 4/6 多帧投票。
- 双眼睁开概率改为主判定，眼睑轮廓不能覆盖明确闭眼结果；不要求用户眨眼或转头。
- 录入改为自然正视采集 10 帧并自动校准；设置页以严格、均衡、便捷三档取代直接修改原始阈值。
- 真机热启动相机到成功中位数约 354 ms；双眼闭合、单眼闭合和快速重锁闭眼回归均保持锁定。
- 新增身份模型、眼睛门槛和多帧稳定器单元测试，CI 同时执行测试、构建和 Lint。

升级说明：从 0.7.x 或更早版本覆盖安装后，需要重新录入一次以生成 v3 双信号模板。普通 RGB 2D 人脸仍可能被本人照片或视频重放绕过，不等同于 Face ID。

## 0.6.0 - 2026-08-20

- 将最低系统版本扩展到 Android 10（API 29），目标覆盖 Android 10–16。
- 增加多版本 AOSP SystemUI 唤醒、用户解析、认证缓存和人脸回调适配。
- 保留“面容光环”和“灵动岛”两套锁屏动画，不显示自拍预览。
- 普通锁屏识别后等待上滑；PIN/密码页识别后直接解锁。
- 快速重新锁屏时清除上一次的弱认证，避免复用旧结果。
- 增加强认证、SIM、设备策略、主用户和关键能力的失败关闭检查。
- 解锁相机入口改用一次性短时会话授权。
- 优化识别路径、灰度缓冲复用、连续三帧判定及异常会话释放。

完整验证范围见 [TEST-REPORT.md](TEST-REPORT.md) 和 [COMPATIBILITY.md](COMPATIBILITY.md)。
