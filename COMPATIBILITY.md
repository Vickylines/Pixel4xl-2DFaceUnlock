# 兼容性矩阵

## 状态定义

- **真机验证**：完成安装、SystemUI 加载、锁屏识别、普通锁屏上滑解锁、PIN 界面直解、快速重锁和异常日志检查。
- **源码适配**：已覆盖对应 AOSP 数据结构和方法签名，并通过 `minSdk 29` 构建/Lint；尚未在该 Android 版本真机验证。
- **安全停用**：平台不在目标范围或关键能力探测失败，不写入认证状态。

| Android | API | 适配状态 | 主要认证路径 |
|---|---:|---|---|
| 10 | 29 | 源码适配 | `SparseBooleanArray`、无参数弱认证检查、单参数人脸回调 |
| 11 | 30 | 源码适配 | 旧/新认证缓存运行时探测 |
| 12 | 31 | 源码适配 | 带强弱标记的认证对象 |
| 13 | 33 | 源码适配 | 认证对象、显式动态广播可见性 |
| 14 | 34 | 源码适配 | 认证对象、AOSP SystemUI 回调 |
| 15 | 35 | 源码适配 | 新版 SystemUI 类名候选与用户解析 |
| 16 | 36 | Pixel 4 XL / LineageOS 23.2 真机验证 | 通用适配器 + coral 内联唤醒补丁 |

## 运行时能力

模块将各能力单独探测，避免一个可选 UI 类缺失导致整个 SystemUI 钩子失败：

1. `KeyguardUpdateMonitor`：必需；缺失时整个模块安全停用。
2. 唤醒路径：方法钩子和屏幕广播任一可用即可，500ms 内重复事件合并。
3. 强认证检查：无法确认系统允许弱生物识别时不启动识别。
4. 认证缓存：只写入探测成功的一个槽位；写入后必须经系统反查确认。
5. 锁屏动画根：缺失时只取消动画，不改变认证判断。
6. 原生锁图标：属于可选增强；旧版没有对应类时不影响识别结果。
7. 相机宿主遮挡保护：缺失时记录诊断，绝不盲目调用未知动画回调。
8. Android 16 相机宿主输入层：SystemUI 以一次性会话令牌校验 Activity token，并关闭跨任务的 `ActivityRecordInputSink`；找不到平台接口时只保留 `1×1 / alpha=0` 安全降级，不伪报触控修复成功。
9. v0.8 身份模型：一次 ML Kit 检测同时提供眼位、睁眼概率和眼鼻嘴位置；同一份 LBP 描述子执行 49 区纹理一致性，几何比例作为独立第二门槛，不增加第二次模型推理。

## 明确不支持

- 非 Google Pixel 设备。
- 访客或辅助用户；当前模板存储未做跨 Android 用户隔离适配，因此安全停用。
- MIUI/HyperOS、One UI、ColorOS 等深度改造的非 AOSP SystemUI。
- Android 9 及更早版本、Android 17 及未来未审计版本。
- 未 Root、没有 LSPosed、没有前置摄像头或禁用相机权限的设备。
- 支付、银行、密码管理器和应用级生物认证。
